# 第四问两个小改法的冻结验证计划

归档状态（2026-09-13）：后台已完成计划中的112条新增运行与全部144条对照轨迹核验。用户随后要求停止继续优化，转入目录整理；本文件保留原计划，当前状态以[README.md](README.md)为准，不自动续跑。

用户于2026-09-13授权实施先①后②，并验证其它情况没有恶化。本轮比较参照是当前shared25（selected），不是较旧joint25；后者仅作为两个退步案例的历史背景。

## 候选与边界

- A：在首次取得单圆保证清除证据后的下一次规划，只给该频道增加前置候选。保留全部扫描和服务任务，比较固定扫描前缀下的最坏相对损失。k=0为敏感性压力假设，不声称即时物理可达。不改变真实覆盖/判空、固定配置、正在执行的多圆、站内扫描承诺。
- B：单独在selected上增加非主目标辅助补测净价值门槛；阳性与阴性都计剩余工作变化，收益大于5秒加0/1秒切频费才执行。主目标与未知覆盖任务保留；独立条件池避免污染原主策略缓存；抽样耗尽保留原行为。具体实现与针对性检查完成后冻结，再开始B运行。
- 不组合A/B，不依据新16例改参数。若方案不达标保留原selected，不用均值掩盖逐例退步。

## 冻结案例与执行

原32例全部纳入回归检查：holdout01的24例、pressure01的8例，包含两个问题案例及原本获胜的案例。由于这些例已经用于诊断，称回归集，不再称独立检验。

新16例：原生成器holdout前8例加seed_offset=2000（945000—945007），pressure全8例加seed_offset=2000（946000—946007）。它们是预先确定的分层自建场景，不代表官方分布。catalog.json在第一次运行前落盘并冻结。在线策略只得到公开状态；真值只供本地环境、审计器和事后报告。

先复核原32条selected的源码指纹、全部动作反馈、独立费用和终局全清，复用为虚拟时间参照。A先运行两个目标案例，然后完整48例；B完成单测/审查后同样独立运行48例。新16例补跑selected。共112条新增完整轨迹，不运行官方接口或正式测试。

每条轨迹保存动作、提案、费用账本、终态和策略元数据；独立审计每步反馈/计费/真值相容，源码指纹包含正式依赖、新候选与运行器。现实时间受并行竞争影响，只作开销诊断，不能由跨批旧wall值宣称精确加速比。

## 采用标准

先全清、无账本或状态审计错误，再逐例比较ΔT和T/N。差异绝对值≤0.0001秒仅作为浮点相等。分别报告回归32、新16、两个目标案例的胜/平/退步数、每个退步秒数、最大退步、平均整局和每源费用、规划耗时及回退。

用户要求其它情况不恶化；出现可复現退步即不声称通过此要求，不自动替换正式selected。即使48例均不退步也只支持该集合，不能证明全部未知情况不会恶化。失败/超时不排除出样本均值，也不能当作全清结果。若发现实现错误则记录原因、保留旧批次，在新批次修复复跑受影响范围；算法效果差不通过事后调阈值伪装实现修复。

复现入口：`py -3.13 -X utf8 experiments/q4/micro_changes/bench.py --prepare`，其后以`--policy A`、`--policy B`及`--policy selected --group new`分别运行。运行器支持按既有完成记录续跑，不覆盖任何已存在结果。
