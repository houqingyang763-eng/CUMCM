# 执行进度

2026-09-12 14:16（本机）：统一动作入口、角度似然、条件粒子再生、1/3层反馈树、完整F1尾部、运行与独立重放入口已实现。5项检查通过。

先用330000均匀/哈希16源场景做冒烟。F3已全清53.952分钟；低预算16次模拟的一层树仍在运行，前段频繁移动。另用缓存未变化频道后验、原地确定清除支配检查的48次/16粒子三层树观察计算与行为；两批源码散列分开，不合并成同版本成绩。

尚未取得新算法完整成绩，尚未进入独立确认集。下一步完成冒烟与四种布局开发检查，先判全清和移动/查漏成本，再决定修正或扩大。

运行目录：runs/smoke_importfix（低预算早期实现），runs/smoke_cached（三层48次，缓存与支配检查）。最早runs/smoke_low只有启动导入错误，无案例成绩。

14:24更新：有限小问题发现相同离散反馈被重复拆成节点，妨碍子节点学习。已按反馈类型/精确舍入角度合并节点，并以观测产生次数保持分支抽样权重；不同角度不合并。小问题的探索系数按其个位/十位秒费用尺度设置，不能直接搬用本任务180秒系数。7项检查通过，其中一层选直接完成、两层选付费观测后按反馈行动。

停止尚未完成的早期运行，保留其全部部分轨迹，不计作完成实验或失败率。runs/smoke_importfix已有F3完整53.952分钟，其他不完整；dev_four中已完成结果单独保留。修正实现进入runs/smoke_merged（同场景U1/T3）与runs/dev_merged（四布局，baseline/F3/T3）。未动独立确认种子。

14:46更新：8项检查通过，另补充已清掉10源但仍有未知频道时，抽样世界剩余0源不能跳过公共查漏。dev_biased增加四个布局的固定偏置误差开发例；不以CPU快慢代替虚拟耗时结论。

当前U1/T3均未整局完成。dev_merged前两个平滑场景及dev_biased四场景的baseline/F3已完成并保留；其余仍运行，progress.json里的success=false不能解读为整局失败，最终以result.json为准。

新增diagnose.py：冻结已执行前缀后用F1完整续行，只作反事实诊断；以及用32个共同条件世界重估首步候选的一层尾部。后者不是三层价值真值。初始几个内侧候选平均差只有十几至七十几秒，配对标准误约四十至六十秒，说明目前无法把具体首点选择说成可靠最优。边界候选一层续行明显更贵，尚未据此改策略。

profile.pstats显示16次三层搜索的叶部完整F1续行占主要时间，覆盖路线与连续证书占大头。运行中的策略继续冻结；diagnose.py和额外测试/审计文件的新增不改变已启动进程的策略行为，但后续新运行需要新manifest目录。

14:59更新：旧随机局面1392164241的第40动作后，一层选补测C20，三层选清除C5。各自只执行这一动作后同接F1，整局分别54.675/54.099分钟；旧F1动作同尾55.618。只是局部诊断，旧完整F3该局51.031更快，未宣称新法已胜。结果在known40_probe/counterfactual.json。

增加唯一结构修正M3：搜索可选整站频道批次，提交到该站完成再规划，原子选择仍保留。目的是检查三条原子指令看不到整站收益的时域限制，并非根据已完成T3总体成绩调参（T3此时仍未完成）。新文件macro_planner.py/run_macro.py，原planner.py和运行中进程不修改。11项检查通过；新增宏动作逐条反馈和费用与实际队列一致性检查。先在同一开发例比较，不使用独立确认集挑设计。

17:05结案：M3八开发例及一个哈希冒烟均全清，但八例均值63.836分钟，旧F3为59.293，baseline为65.860。M3相对F3一胜七负，最坏多14.500分钟；不达原门槛，停止当前方向加功能。U1冒烟、六T3开发例触及各自两小时本地计算上限，结果和未全清状态保留。两项后启动T3开发例和T3冒烟主动停止，不能把已花时间当完成成绩。

17:09用户再次明确要求终止无法继续的实验、分析提升。已复核没有本轮运行进程。仅整理独立账本、逐站审查和新设计分析；没有启动新的优化实验、正式测试、24例确认或8例压力。结论见REPORT.md，下一步研究建议见NEXT_DESIGN.md。明确保留POMDP理论可行性与本次有限实现的证据缺口。
