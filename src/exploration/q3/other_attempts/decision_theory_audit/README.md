# 已完成的冻结决策诊断

先读 [理论与跨方法判断](THEORY_REPORT.md)，本轮固定状态试验详见 [DIAGNOSTIC_REPORT.md](DIAGNOSTIC_REPORT.md)。

本目录保存2026-09-13的一次已完成诊断：固定8个开发案例、每例旧轨迹第一处成功多候选决策、共441次同F1续行。`PLAN.json` 固定输入和抽样，`SUMMARY.json` 保存汇总，`cases/` 保存每候选每世界结果及完整控制器检查点。

`probe.py` **没有 `--out` 参数，直接再次运行会覆盖此目录的既有PLAN和结果**。不要为复核而直接重跑。当前安全复核命令如下，它只读取本轮结果与原始输入，不新增模拟，也不重写核验记录：

```powershell
py -3.13 experiments/b_q3/decision_theory_audit/verify.py --read-only
```

报告中的生成命令是本次运行记录。若未来需要独立复现，应先给运行器增加隔离输出位置，保留本次实际执行的 `probe.py` 源码与哈希，或在同层级隔离实验目录中运行源码副本并核实 `ROOT` 仍指向仓库根目录。不能把未隔离的第二次输出当作本次原始证据。

`checkpoint.pkl` 是本工具自产的完整状态副本，普通复核无需加载。所有原算法、既有实验结果和官方输入均保持只读；本诊断没有运行官方模拟器，没有修改F3或运行、修改P1。同期完成的P1轻量结果仅在理论报告中作只读引用。
