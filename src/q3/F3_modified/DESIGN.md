# F3改：原普通24例配对复测

2026-09-13，用户指定名称并授权扩展至原F3普通24例。F3改是已完成第三轮的R：F3 + A + 扫描站挪近。实现仍为 `RunContext('A') + relocation_context + RelocationPolicy`；本轮不改算法、候选数、覆盖验收或10次二分，不加入B及换侧删站。

## 固定输入和复用

选用 `experiments/b_q3/refinement/runs/micro_confirm/cases.json` 的全部24例（320100—320123）：均匀、边缘、聚集、直线四种布局，各有平滑、偏置、散列三种误差，每组合两例。这是原F3的后一批普通案例，另外存在独立压力组。

此前8例是上述24例的固定子集，索引0、3、6、9、12、15、18、21。校验案例、源码、开局缓存与旧产物指纹后，引用已有R的8条轨迹；其余16例新跑。全部24条F3轨迹引用原批次，不重跑、不混入另一批310100开头的案例。开局选择均复用同案公开首站历史的原缓存，核对history_seed并单列历史选点计算时间。

每例上限3000动作、10800秒虚拟时间、1200秒含缓存的计算时间；独立进程两路执行。冻结完成后不因结果调整参数、剔除坏例或改统计口径。真值仅供环境生成反馈和事后审计，不进入策略选择。

## 运行前固定的分析口径

1. 先检查24/24是否全部清除，失败案例完整保留；任一方法失败，不用仅成功子集宣布更快。
2. 主比较为24局总虚拟时间之和、单局均值、每局T/N再等权平均。附列sum(T)/sum(N)，避免混淆每源加权方式。这里每源时间包括完成整局所需的查漏；不是每次清除动作的5秒。
3. 逐例配对差定义为F3减F3改，正数表示改进。列胜/平/负（浮点容差1e-6秒）、中位数、最坏退步、逐例删除后的均值区间。
4. 完整24例、原8例、新增16例分别报告，尤其看新增16例是否仍有收益。24例包含开发样本，不能称为全新独立验证。
5. 配对且按四种布局分层的bootstrap，固定随机种子20260913、20000次，百分位95%区间；主指标为平均整局节省与平均每源节省。不将描述性分组均值当作因果效应或隐藏测试表现。若区间跨零，表述为改善证据不明确。
6. 实用幅度同时看百分比与原开发筛选的平均整局30秒门槛。统计区间不等于实用价值；仅超过30秒也不等于统计证据清晰。
7. 全量48条新旧轨迹重放反馈、几何约束和计费，逐项分解移动/测量/切频/清除成本；尾段仅作辅助，不能代替整局指标。

全部为本地自建案例，不访问官方模拟器。本次是固定方案的覆盖面验证，不是调参轮。
