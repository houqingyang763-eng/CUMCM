# 两方案的公共运行依赖

这里保存从原第三问目录实际搬入的共用原文件；保留原相对路径形状以便追溯。

- `adaptive_second/adaptive.py`：第一站公开反馈后的第二站选择与原续行。
- `refinement/posterior.py`：几何可行域条件下的有限场景构造。
- `probability_patrol/`：公共状态、连续覆盖路线、原本地环境执行器、巡检基类及固定配置。

实际文件位于`experiments/b_q3/`子树。历史方案也曾引用这些文件，它们现在只保留这一份原件；[统一入口](../runtime.py)负责临时恢复旧工程结构。不要从这里单独改导入路径，否则会改变被冻结的原字节。
